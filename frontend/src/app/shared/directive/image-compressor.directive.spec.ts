import { ImageCompressorDirective } from './image-compressor.directive';

describe('ImageCompressorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageCompressorDirective();
    expect(directive).toBeTruthy();
  });
});
