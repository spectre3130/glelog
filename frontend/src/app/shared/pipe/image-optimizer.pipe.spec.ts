import { ImageOptimizerPipe } from './image-optimizer.pipe';

describe('ImageOptimizerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageOptimizerPipe();
    expect(pipe).toBeTruthy();
  });
});
